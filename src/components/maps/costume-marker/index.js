import {StyleSheet, View} from 'react-native';
import React from 'react';
import {LocationSlash} from 'iconsax-react-native';

const CostumeMarker = () => {
  return (
    <View>
      <LocationSlash size={40} color={'#65B741'} variant="Bold" />
    </View>
  );
};

export default CostumeMarker;

const styles = StyleSheet.create({});
